class Statistics:

    def __init__(self, averagePerClient, tourPercentage, topClients, topShips, topProducts):
        self.averagePerClient = averagePerClient
        self.tourPercentage = tourPercentage
        self.topClients = topClients
        self.topShips = topShips
        self.topProducts = topProducts